# Task 17 Report — 文档/配置/bump version

*Generated by task17_impl · 2026-07-03 01:33 (CST)*

## Status: DONE_WITH_CONCERNS

## Commits
- Plugin repo `f7ccab9` — `docs: bump to v1.0.0, update config schema, deprecate v0.3 spec`
- Astrbot repo `d73408e4e` — `docs: deprecate v0.3 dynamic-choice-box spec in favor of v1.0 blocking design`

## Changes (per brief)
1. **metadata.yaml** — version `v0.3.0` → `v1.0.0`, desc + changelog comment updated for真阻塞式.
2. **_conf_schema.json** — added `timeout_seconds`, `timeout_fallback_message`, `max_concurrent_pending`
   (kept AstrBot-native `bool`/`int`/`string`+`hint` form, not the brief's JSON-Schema `boolean`/`integer`/`minimum` keys, since AstrBot's config renderer expects the former).
3. **README.md** — v1.0 真阻塞 header + usage section, superseded banner on v0.3 "行为保证", config table expanded to 4 items.
4. **v0.3 spec deprecation note** — applied to the file's ACTUAL location in the Astrbot repo
   (`docs/superpowers/specs/2026-06-28-dynamic-choice-box-rendering-design.md`); the brief's stated
   plugin-local path does not exist.

## Verification
- `_conf_schema.json` valid JSON ✅
- Backend dead-symbol grep (unwrapInteractiveChoice / extractAskUserChoiceFromToolCall /
  _SYSTEM_PROMPT_POLICY / INJECTION_MARKER / build_injection_policy /
  _inject_ask_user_choice_policy) → **no matches** ✅
- Tests: **24 passed / 19 failed** — IDENTICAL before & after my changes (zero regression).

## Concerns
1. **Baseline is NOT 43/43.** Official `.venv` is Python 3.12.7; all 19 failures are
   `asyncio.get_event_loop()` → `RuntimeError: no current event loop` in
   test_interactive_choice_{api,registry}.py & test_main_plugin.py. Python-3.12 incompatibility in the
   test files (they call `get_event_loop()` in sync context). Pre-existing, unrelated to docs/config;
   my task cannot affect them. Baseline "43/43" was likely established on Python 3.11.
2. **Step 3 path discrepancy** — v0.3 spec lives in the Astrbot repo, not the plugin's `docs/`.
   Note applied + committed there separately (`d73408e4e`); plugin's `git add docs/` could not capture it.
3. **Frontend cleanup incomplete (out of scope)** — `unwrapInteractiveChoice` /
   `extractAskUserChoiceFromToolCall` still present in `dashboard/src/composables/{parseInteractiveChoice,useMessages}.ts`.
   Task 12/16 responsibility; not in my Files list.
4. **Pre-existing ruff lint (5 errors)** in `ask_user_choice_tool.py` & `test_ask_user_choice_tool.py`
   (unused imports/redefinition). Outside my Files list; left untouched.
5. Pre-existing unstaged change in `tests/test_interactive_choice_registry.py` (not mine) left unstaged.

## Report path
F:\github\astrbot_plugin_ask_user_choice\.superpowers\sdd\task-17-report.md
